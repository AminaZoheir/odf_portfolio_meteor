{
  "servers": [
    {
      "host": "odf-eg.com",
      "username": "ubuntu",
      "sshOptions": { "port" : 24522 },
      "pem": "./odf.com_rsa"
    }
  ],

  "setupMongo": true,
  "setupNode": true,
  "nodeVersion": "0.10.40",
  "setupPhantom": true,
  "enableUploadProgressBar": true,
  "appName": "odf",
  "app": "/home/mnzaki/shoghl.git/hosted/odf_portfolio_meteor/odf-portfolio",

  // Configure environment
  "env": {
    "ROOT_URL": "http://odf.com",
    "PORT": "3000",
    "UPSTART_UID": "meteoruser"
  },

  // Meteor Up checks if the app comes online just after the deployment
  // before mup checks that, it will wait for no. of seconds configured below
  "deployCheckWaitTime": 15
}
